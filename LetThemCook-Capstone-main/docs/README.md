# Documentation folder

This folder stores legacy handoff notes and unused imported design assets so the app root stays clean.

- `frontend-handoff/` contains old frontend conversion, checklist, and summary documents.
- `frontend-assets/` contains Figma/imported screenshots that are not required by the running app.
- `README_MERGED.md` is the original merged-project README retained for reference.
